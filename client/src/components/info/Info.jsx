import React from 'react';
import PropTypes from 'prop-types';

Info.propTypes = {
    
};

function Info(props) {
    return (
        <div>
            Private Page
        </div>
    );
}

export default Info;